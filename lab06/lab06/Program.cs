using lab06_1;
using System.Collections;

namespace lab06
{
    internal class Program
    {
        static void Main(string[] args)
        {
            ArrayList arr = new ArrayList();
            {
                arr.Add(new product(A, 10.5, 1);
                arr.Add(new product(B, 9.5, 2);
                arr.Add(new product(C, 11.5, 3);
                arr.Add(new product(D, 2.5, 4));
            }//in ra list:
            foreach(var item in arr)
            {
                Console.WriteLine("" + item);
            }
        }
    }
}
